[build-system]
requires = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name = "yt-dlw-rs"
version = "0.0.1"
requires-python = ">=3.11"

endencies = [
  "yt-dlp == 2023.7.6",
  "websockets==11.0.3"
]
